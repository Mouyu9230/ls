## 使用C语言基于GNU/Linux操作系统，编程实现ls命令的部分功能。  
>**验收要求**  
1.实现 ls 的 -a、-l、-R、-t、-r、-i、-s 参数，并允许这些参数任意组合。  
2.-R 需要对 / 的遍历测试。  
3.界面美观（输出对齐、带颜色显示等）。  
4.无资源与内存泄漏。  
5.开发过程记录、总结并发布在个人博客中。  

# 使用方式  
终端输入：  
>cd ls  
gcc ls.c -o ls  
sudo ./ls (参数与目录)  
